import _pt from "prop-types";
import React from 'react';
import cx from 'classnames';
import { Group } from '@vx/group';
export default function Links(_ref) {
  var _ref$links = _ref.links,
      links = _ref$links === void 0 ? [] : _ref$links,
      linkComponent = _ref.linkComponent,
      className = _ref.className;
  return React.createElement(React.Fragment, null, links.map(function (link, i) {
    return React.createElement(Group, {
      key: "network-link-" + i,
      className: cx('vx-network-link', className)
    }, React.createElement(linkComponent, {
      link: link
    }));
  }));
}
Links.propTypes = {
  links: _pt.arrayOf(_pt.any),
  linkComponent: _pt.oneOfType([_pt.string, _pt.any, _pt.elementType]).isRequired,
  className: _pt.string
};